import { create } from '@storybook/theming/create';

const storybookTheme = create({
  base: 'dark',
  brandTitle: '0xDevs',
  brandUrl: 'https://0xdevs.com',
  brandTarget: '_self',
  //    brandImage: '',
});

export default storybookTheme;
